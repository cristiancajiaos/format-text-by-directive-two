import { FormatText } from './format-text';

describe('FormatText', () => {
  it('should create an instance', () => {
    const directive = new FormatText();
    expect(directive).toBeTruthy();
  });
});
